# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Environment / secrets
.env

# Per-machine dev settings (each teammate picks their own local model)
.sentinel/

# Credentials / installers / macOS cruft — never commit
*_accessKeys.csv
*.csv.download
*.pkg
.DS_Store

# Node / frontend
node_modules/
dist/
build/
*.tsbuildinfo

# Editors
.vscode/
.idea/

# Logs
*.log

# Jupyter
.ipynb_checkpoints/

# Internal dev/build-process docs — not part of the submission
/CLAUDE.md
/instructions.md
/kickoff-prompt.md
/tasks.md
/website-prompt.md

# Stale duplicate config — the real one is /sentinel.config.json
# (sentinel/shared/config_file.py only ever reads the repo-root copy)
/sentinel/sentinel.config.json
